/// <reference types="cypress" />

describe('Test description', ()=>{
    it('test title', ()=>{
        cy.visit('http://localhost:3000') // visits the local host, backend and front end needs to be running
      
    })
})